# ═══════════════════════════════════════════════════════════
# TROCABAIRRO · SUPABASE
# ═══════════════════════════════════════════════════════════
# Copie este arquivo para .env.local e preencha com as chaves
# do seu projeto em https://supabase.com/dashboard/settings/api
#
# ⚡ INTERRUPTOR INTELIGENTE:
#   • Chaves preenchidas → app roda nativo no Supabase
#     (Postgres + Auth + Storage nos buckets 'ads' e 'avatars')
#   • Chaves vazias/ausentes → app roda em MODO DEMO via
#     localStorage (dados de exemplo, sem backend)
#
# 1) Execute supabase/schema.sql no SQL Editor do Supabase
# 2) Crie sua conta em /cadastro
# 3) Promova-se a admin:
#      update public.profiles set role = 'admin'
#      where email = 'seu-email@exemplo.com';
# 4) Acesse /admin e /admin/cms
#
# 🚀 VERCEL (produção) — nomes EXATOS das variáveis:
#   Dashboard Vercel → seu projeto → Settings → Environment
#   Variables → adicione, nos escopos Production/Preview/Development:
#     NEXT_PUBLIC_SUPABASE_URL      = https://<projeto>.supabase.co
#     NEXT_PUBLIC_SUPABASE_ANON_KEY = eyJ... (chave pública anon)
#   Obs.: NEXT_PUBLIC_* são congeladas no BUILD — depois de mudar,
#   faça um novo deploy (Deploy → Redeploy) para o app em produção
#   passar a rodar no Supabase. Confirme em: /api/health →
#   { "mode": "supabase" }
# ═══════════════════════════════════════════════════════════

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# ═══════════════════════════════════════════════════════════
# 💳 MERCADO PAGO — Checkout Pro (PIX/cartão) · produção real
#
# Sem estas variáveis o site continua funcionando com a
# simulação local (fallback dev). Com elas, os botões de
# /planos e /impulsionar redirecionam ao Mercado Pago e o
# webhook (/api/mercadopago/webhook) ativa os planos após a
# aprovação do pagamento.
#
# Na Vercel: Settings → Environment Variables (todos os escopos):
#   MERCADOPAGO_ACCESS_TOKEN  = TEST-... (teste) ou APP_USR-... (produção)
#   APP_URL                   = https://SEU-DOMINIO.vercel.app
#   SUPABASE_SERVICE_ROLE_KEY = eyJ... (Settings → API do Supabase —
#                               NUNCA usar em NEXT_PUBLIC_*)
# ═══════════════════════════════════════════════════════════

MERCADOPAGO_ACCESS_TOKEN=
APP_URL=
SUPABASE_SERVICE_ROLE_KEY=
